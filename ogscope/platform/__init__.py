"""
平台与基础设施层 / Platform and infrastructure layer.

聚合设备驱动、硬件平面编排、边界适配，以及本地 SPI 界面与 INDI 等集成预留，
与 `domain`（业务）、`web`（HTTP 交付）区分。
Groups device drivers, hardware-plane orchestration, boundary adapters, and placeholders
for SPI UI and INDI integration, distinct from `domain` (business) and `web` (HTTP delivery).
"""
