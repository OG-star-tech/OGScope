# Tetra3 图案库 / Pattern database

将 `default_database.npz` 放置于此目录，或通过环境变量 `OGSCOPE_SOLVER_TETRA_DATABASE_PATH` 指向绝对路径。

可从已安装的 `cedar-solve` wheel 中复制 `tetra3/data/default_database.npz`，或自行运行 `generate_database` 生成。
